<tinylog>
    <!-- Set the global logging level -->
    <level>info</level>

    <!-- Define a console writer -->
    <writer>
        <console>
            <format>{date:yyyy-MM-dd HH:mm:ss} [{thread}] {level}: {message}</format>
        </console>
    </writer>
</tinylog>
